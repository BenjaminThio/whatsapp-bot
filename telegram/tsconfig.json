{
  "extends": "../tsconfig.base.json",
  "compilerOptions": {
    "outDir": "./dist",
    // Widened to the workspace root: this project imports ../shared/**.
    "rootDir": ".."
  },
  "include": ["src/**/*.ts", "api/**/*.ts", "scripts/**/*.ts", "../shared/**/*.ts"],
  "exclude": ["dist", "node_modules", "../node_modules", "src/pixelforge/build", "../shared/dist"]
}
