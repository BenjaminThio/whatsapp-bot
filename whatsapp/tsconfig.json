{
  "extends": "../tsconfig.base.json",
  "compilerOptions": {
    "outDir": "./dist",
    // Widened to the workspace root: this project imports ../shared/**, and
    // rootDir must contain every file in the program.
    "rootDir": ".."
  },
  "include": ["src/**/*.ts", "../shared/**/*.ts"],
  "exclude": ["dist", "node_modules", "../node_modules", "../shared/dist"]
}
